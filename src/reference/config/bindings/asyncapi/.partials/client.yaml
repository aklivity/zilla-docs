  asyncapi_client:
    type: asyncapi
    kind: client
    options:
      specs:
        my-kafka-api-spec:
          servers:
            - kafka://localhost:9092
          catalog:
            my_catalog:
              subject: petstore
              version: latest
