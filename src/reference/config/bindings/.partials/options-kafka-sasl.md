<!-- markdownlint-disable MD024 -->
#### sasl.mechanism

> `enum` [ `plain`, `scram-sha-1`, `scram-sha-256`, `scram-sha-512` ] | Deprecated

Supports `plain` and `scram` mechanisms. Deprecated in favor of `kafka.authorization`.

##### sasl.username\*

> `string`

SASL username.

##### sasl.password\*

> `string`

SASL password.
